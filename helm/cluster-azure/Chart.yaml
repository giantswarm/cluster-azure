apiVersion: v2
name: cluster-azure
description: A helm chart for creating Cluster API clusters with the Azure infrastructure provider (CAPZ).
type: application
version: [[.Version]]
annotations:
  application.giantswarm.io/team: "glippy"
restrictions:
  compatibleProviders:
    - azure
dependencies:
  - name: cluster-shared
    version: "0.3.0"
    repository: "https://giantswarm.github.io/cluster-catalog"
