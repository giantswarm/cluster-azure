# DO NOT EDIT. Generated with:
#
#    devctl@5.19.1-dev
#

# This workflow checks that the values.schema.json fulfills the requirements
# we impose on values.schema.json of cluster-apps with the help of schemalint.
# It also generates values.yaml based on the defaults specified in 
# values.schema.json with the help of helm-values-gen.

name: JSON schema validation
on: push

jobs:
  validate:
    name: Verify values.schema.json with schemalint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run schemalint
        id: run-schemalint
        uses: giantswarm/schemalint/actions/verify-helm-schema@v1
        with:
          rule-set: 'cluster-app'
  generate:
    name: Check that values.yaml is generated from values.schema.json with helm-values-gen
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run helm-values-gen
        id: run-helm-values-gen
        uses: giantswarm/helm-values-gen/actions/ensure-generated@v1
